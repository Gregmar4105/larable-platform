services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: larable_app
    ports:
      - "8080:10000"
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: base64:0/eW30dO/2b2zK96cZ9pS823dY4sP90aB1234567890=
      APP_URL: http://localhost:8080
      DB_CONNECTION: sqlite
      RUN_MIGRATIONS: "true"
